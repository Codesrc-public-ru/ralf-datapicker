TASK-017 done.
Added controlled draft state to `useDatePickerInput` with sync from external `value` and guarded overwrite rules.
DatePicker input now tracks focus and draft edits through the hook, so the typed text can drift from selected value without losing controlled sync.
Validation note: `npm run test:unit`, `npm run lint`, `npm run typecheck`, and `npm test` passed.

TASK-041 done.
Added a separate `typecheck` gate with `tsconfig.json` and `npm run typecheck`.
Added CSS module and test globals declarations so TypeScript checks the DatePicker source cleanly.
Extended the date-picker test runner to transpile TypeScript modules before eval, so runtime tests still pass.
Validation note: `npm run typecheck`, `npm run lint`, and `npm test` passed.

TASK-040 done.
ESLint gate already exist in `eslint.config.cjs`.
It covers source and test files for TypeScript, React, hooks, import order, and jsx-a11y rules.
`npm run lint` pass.
TASK status now done.

TASK-021 done.
Keyboard engine now lives in `src/components/date-picker/model/useDatePickerKeyboard.ts`.
It resolves Arrow keys, Home/End, PageUp/PageDown, Shift+PageUp/Down, Enter, Space, and Escape into calendar actions.
Keyboard constants are centralized in `src/components/date-picker/constants/keyboard.ts`.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed.
Validation note: `uv run ruff check .` and `uv run pytest` could not run because `uv` is not installed in this workspace.

TASK-011 done.
Validation layer now lives in `src/components/date-picker/model/validation.ts`.
It resolves one error at a time in the order format -> range -> external -> required.
State now carries `isInvalid` alongside `isVisible`.
Validation checks reuse `isDateInRange` and `formatInputDate` for message text.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed.
Validation note: `uv run ruff check .` and `uv run pytest` could not run because `uv` is not installed in this workspace.

TASK-009 done.
i18n helpers now exist in lib/i18n.
formatMonthLabel, getMonthYearLabel, formatWeekdayLabel, formatFullDateLabel, getWeekdayNames, and getFirstDayOfWeek use Intl and date-only normalization.
Locale checks cover en-US and de-DE.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed.
Validation note: `uv run ruff check .` and `uv run pytest` could not run because `uv` is not installed in this workspace.

TASK-006 done.
Date movement helpers are in place.
addDays, addMonths, and addYears now clamp date-only math correctly.
Added navigation helpers for Home, End, PageUp, PageDown, and Shift+PageUp/Down.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed.
Validation note: `uv run ruff check .` and `uv run pytest` could not run here because `uv` is not installed in this workspace.
TASK-002 done.
DatePicker type layer is split now.
Public props use shared aliases.
Internal state has clear input, calendar, keyboard, focus, live region, and validation models.
Next step: build validation and parsing behavior on top.
TASK-003 done.
Test scaffold is live.
Runner covers unit, integration, and a11y source checks.
Next step: add real React DOM test deps when project gets them.
TASK-004 done.
Date-only helpers now exist in lib/date.
normalizeDate makes a local day clone.
compareByDay gives -1/0/1 by calendar day.
isSameDay, isSameMonth, and getToday use date-only logic.
Next step: build month matrix and week-start helpers on top.
Validation note: `uv`, `ruff`, and `pytest` are not installed in this workspace. Node date-picker unit harness passed instead.
TASK-005 done.
Month math now exists in startOfWeek, getFirstDayOfWeek, and buildMonthMatrix.
Grid starts from locale week start and builds a 6x7 month matrix.
Next step: wire this grid into calendar UI and month navigation.
Validation note: `uv run ruff check .` and `uv run pytest` both fail here because `uv` is missing. Local node harness passed.
TASK-007 done.
Range and disabled helpers now work in lib/date.
isDateInRange now compares date-only values and treats reversed bounds as invalid.
isDateDisabled now checks min/max first, then disabledDates arrays and predicates with normalized days.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed. `uv run ruff check .` and `uv run pytest` could not run here because `uv` is not installed.
TASK-008 done.
Input helpers now live in lib/input.
sanitizeInputValue normalizes pasted separators and keeps invalid draft text visible.
parseInputDate now splits empty, partial, valid, format, and calendar states.
formatInputDate now returns canonical DD.MM.YYYY text with zero padding.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed. `uv run ruff check .`, `uv run pytest`, and `tsc --noEmit` could not run here because those tools are not installed in this workspace.

TASK-010 done.
a11y helpers now live in lib/a11y with real behavior.
getTriggerAriaLabel speaks empty vs selected state with locale-aware full date.
getDayAriaLabel speaks selected, unavailable, and outside month states.
getInputDescribedBy composes and dedupes ids.
getLiveRegionMessage joins announcement parts cleanly.
getDialogAriaProps returns dialog role, aria-modal, title id, and hidden state for closed dialog.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed. `uv run ruff check .` and `uv run pytest` both fail because `uv` is not installed in this workspace.

TASK-015 done.
UI layer now has real presentational components for DatePickerField, DatePickerTrigger, DatePickerError, DatePickerDialog, CalendarHeader, CalendarWeekdays, CalendarGrid, and CalendarDayCell.
CSS Modules now cover field, dialog, header, grid, cell, button, weekday, and state styles.
UI layer stays dumb. No lib/model imports from ui files.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `npm run lint -- --max-warnings=0` passed.
TASK-014 done.
Public DatePicker shell now renders the controlled field wrapper, read-only input, trigger, and error slot. The shell pulls display text from `formatInputDate`, trigger text from `getTriggerAriaLabel`, and described-by wiring from `getInputDescribedBy`.
Next step: wire open/close state and calendar dialog behavior in the next task chain.

TASK-016 done.
DatePicker now toggles calendar dialog from trigger click.
`useDatePickerState` now keeps open/close actions, `visibleMonth`, and focused day state.
Open month choice follows selected value when present, else current month from `getToday`.
DatePicker shell now renders dialog header with month-year label and trigger aria-expanded / aria-haspopup wiring.
Validation note: `npm run lint`, `npm run typecheck`, and `npm test` passed.
