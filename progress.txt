TASK-002 done.
Shared internal type layer now lives in `src/components/date-picker/types/internal.types.ts`.
Moved hook and UI prop types out of component files, and `validation.ts` now reuses the shared input model.
Validation note: `npm run lint`, `npm run typecheck`, and `npm test` passed.

TASK-035 done.
Added real keyboard integration flow in `keyboard-navigation.test.tsx`.
Test now proves open focus, ArrowRight, PageDown, Enter, and Escape in browser-like harness.
Validation note: `node scripts/run-date-picker-tests.mjs all`, `npm run lint`, and `npm run typecheck` passed.

TASK-030 done.
Removed Date mutation from `parseInputDate` and raw Date identity compare from `useDatePickerState`.
State now keeps focused day and visible month on date-only helpers only.
Validation note: `npm test -- --runInBand`, `npm run lint`, and `npm run typecheck` passed.
Next step: TASK-029 if security hardening is next.

TASK-030 done.
Month-start logic now lives in `lib/date/startOfMonth` and is used by state and keyboard hooks.
Added regression coverage that rejects raw timestamp APIs and Date mutation calls inside `src/components/date-picker`.
Validation note: `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run test:integration`, and `npm run test:a11y` passed.
Next step: TASK-032 if date-helper unit coverage is next.

TASK-044 done.
Vite app shell now live with `index.html`, `vite.config.ts`, `src/main.tsx`, `src/App.tsx`, and `src/App.module.css`.
`package.json` now exposes `dev`, `build`, and `preview` through Vite.
Fixed browser fallback check so focus return and selected-day aria state validate right element.
Validation note: `npm run lint`, `npm run typecheck`, `node scripts/validate-date-picker-structure.mjs`, and `npm test` passed.

TASK-026 done.
Day cell states now have distinct visual treatment in `DatePicker.module.css` for selected, focused, unavailable, and outsideMonth.
Added DOM coverage in `src/components/date-picker/tests/dom-stack.test.tsx` for selected, focused, unavailable, and outside-month render states.
Validation note: `npm run test:integration`, `npm test`, `npm run lint`, and `npm run typecheck` passed.
Next step: TASK-027 if locale integration is the next priority.

TASK-041 sync done.
Typecheck gate already exists in `package.json` and `tsconfig.json`.
Verified with `npm run typecheck`; pass.
Updated TASK-041 status in `tasks.json`.

TASK-041 done.
Typecheck gate already existed in `package.json` and `tsconfig.json`.
Ran `npm run typecheck` and it passed.
TASK status now done.

TASK-023 done.
Added dialog focus trap in `useDatePickerFocus` with Tab and Shift+Tab cycling through enabled day buttons.
DatePicker now wires dialog `onKeyDown` to the focus hook, while initial open focus still lands on the selected day or today through the keyboard focus sync.
Validation note: `npm run lint`, `npm run typecheck`, `npm test`, and `npm run test:integration` passed.

TASK-022 done.
Keyboard engine now wired to calendar grid.
Grid gets `onKeyDown`.
Day cells get roving `tabIndex`, focus refs, and focus sync on day focus.
Enter/Space select active day. Escape closes dialog.
Validation note: `npm run lint`, `npm run typecheck`, `npm test`, `npm run test:integration`, and `npm run test:unit` passed.

TASK-017 done.
Added controlled draft state to `useDatePickerInput` with sync from external `value` and guarded overwrite rules.
DatePicker input now tracks focus and draft edits through the hook, so the typed text can drift from selected value without losing controlled sync.
Validation note: `npm run test:unit`, `npm run lint`, `npm run typecheck`, and `npm test` passed.

TASK-041 done.
Added a separate `typecheck` gate with `tsconfig.json` and `npm run typecheck`.
Added CSS module and test globals declarations so TypeScript checks the DatePicker source cleanly.
Extended the date-picker test runner to transpile TypeScript modules before eval, so runtime tests still pass.
Validation note: `npm run typecheck`, `npm run lint`, and `npm test` passed.

TASK-040 done.
ESLint gate already exist in `eslint.config.cjs`.
It covers source and test files for TypeScript, React, hooks, import order, and jsx-a11y rules.
`npm run lint` pass.
TASK status now done.

TASK-021 done.
Keyboard engine now lives in `src/components/date-picker/model/useDatePickerKeyboard.ts`.
It resolves Arrow keys, Home/End, PageUp/PageDown, Shift+PageUp/Down, Enter, Space, and Escape into calendar actions.
Keyboard constants are centralized in `src/components/date-picker/constants/keyboard.ts`.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed.
Validation note: `uv run ruff check .` and `uv run pytest` could not run because `uv` is not installed in this workspace.

TASK-011 done.
Validation layer now lives in `src/components/date-picker/model/validation.ts`.
It resolves one error at a time in the order format -> range -> external -> required.
State now carries `isInvalid` alongside `isVisible`.
Validation checks reuse `isDateInRange` and `formatInputDate` for message text.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed.
Validation note: `uv run ruff check .` and `uv run pytest` could not run because `uv` is not installed in this workspace.

TASK-009 done.
i18n helpers now exist in lib/i18n.
formatMonthLabel, getMonthYearLabel, formatWeekdayLabel, formatFullDateLabel, getWeekdayNames, and getFirstDayOfWeek use Intl and date-only normalization.
Locale checks cover en-US and de-DE.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed.
Validation note: `uv run ruff check .` and `uv run pytest` could not run because `uv` is not installed in this workspace.

TASK-006 done.
Date movement helpers are in place.
addDays, addMonths, and addYears now clamp date-only math correctly.
Added navigation helpers for Home, End, PageUp, PageDown, and Shift+PageUp/Down.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed.
Validation note: `uv run ruff check .` and `uv run pytest` could not run here because `uv` is not installed in this workspace.
TASK-002 done.
DatePicker type layer is split now.
Public props use shared aliases.
Internal state has clear input, calendar, keyboard, focus, live region, and validation models.
Next step: build validation and parsing behavior on top.
TASK-003 done.
Test scaffold is live.
Runner covers unit, integration, and a11y source checks.
Next step: add real React DOM test deps when project gets them.
TASK-004 done.
Date-only helpers now exist in lib/date.
normalizeDate makes a local day clone.
compareByDay gives -1/0/1 by calendar day.
isSameDay, isSameMonth, and getToday use date-only logic.
Next step: build month matrix and week-start helpers on top.
Validation note: `uv`, `ruff`, and `pytest` are not installed in this workspace. Node date-picker unit harness passed instead.
TASK-005 done.
Month math now exists in startOfWeek, getFirstDayOfWeek, and buildMonthMatrix.
Grid starts from locale week start and builds a 6x7 month matrix.
Next step: wire this grid into calendar UI and month navigation.
Validation note: `uv run ruff check .` and `uv run pytest` both fail here because `uv` is missing. Local node harness passed.
TASK-007 done.
Range and disabled helpers now work in lib/date.
isDateInRange now compares date-only values and treats reversed bounds as invalid.
isDateDisabled now checks min/max first, then disabledDates arrays and predicates with normalized days.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed. `uv run ruff check .` and `uv run pytest` could not run here because `uv` is not installed.
TASK-008 done.
Input helpers now live in lib/input.
sanitizeInputValue normalizes pasted separators and keeps invalid draft text visible.
parseInputDate now splits empty, partial, valid, format, and calendar states.
formatInputDate now returns canonical DD.MM.YYYY text with zero padding.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed. `uv run ruff check .`, `uv run pytest`, and `tsc --noEmit` could not run here because those tools are not installed in this workspace.

TASK-010 done.
a11y helpers now live in lib/a11y with real behavior.
getTriggerAriaLabel speaks empty vs selected state with locale-aware full date.
getDayAriaLabel speaks selected, unavailable, and outside month states.
getInputDescribedBy composes and dedupes ids.
getLiveRegionMessage joins announcement parts cleanly.
getDialogAriaProps returns dialog role, aria-modal, title id, and hidden state for closed dialog.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `node scripts/validate-date-picker-structure.mjs` passed. `uv run ruff check .` and `uv run pytest` both fail because `uv` is not installed in this workspace.

TASK-015 done.
UI layer now has real presentational components for DatePickerField, DatePickerTrigger, DatePickerError, DatePickerDialog, CalendarHeader, CalendarWeekdays, CalendarGrid, and CalendarDayCell.
CSS Modules now cover field, dialog, header, grid, cell, button, weekday, and state styles.
UI layer stays dumb. No lib/model imports from ui files.
Validation note: `node scripts/run-date-picker-tests.mjs all` and `npm run lint -- --max-warnings=0` passed.
TASK-014 done.
Public DatePicker shell now renders the controlled field wrapper, read-only input, trigger, and error slot. The shell pulls display text from `formatInputDate`, trigger text from `getTriggerAriaLabel`, and described-by wiring from `getInputDescribedBy`.
Next step: wire open/close state and calendar dialog behavior in the next task chain.

TASK-016 done.
DatePicker now toggles calendar dialog from trigger click.
`useDatePickerState` now keeps open/close actions, `visibleMonth`, and focused day state.
Open month choice follows selected value when present, else current month from `getToday`.
DatePicker shell now renders dialog header with month-year label and trigger aria-expanded / aria-haspopup wiring.
Validation note: `npm run lint`, `npm run typecheck`, and `npm test` passed.

TASK-020 done.
DatePicker now renders the month grid in the shell and wires day click to `useDatePickerSelection`.
Available day click calls `onChange` with normalized date and closes dialog right away.
Validation note: `npm run lint`, `npm run typecheck`, `npm test`, `npm run test:integration`, and `npm run test:a11y` passed.

TASK-018 done.
Manual input now commits valid dates through `onChange(Date)` and full clear through `onChange(null)`.
Draft text stays visible while editing, even when the controlled value still points to the old date.
Next step: wire blur-driven error display and submit/external invalid states on top of this input commit flow.
Validation note: `npm run test:unit`, `npm run lint`, `npm run typecheck`, and `npm test` passed.

TASK-025 done.
Keyboard navigation now respects `minDate`, `maxDate`, and `disabledDates` when moving focus. `resolveKeyboardNavigation` blocks movement into unavailable targets, so focus stays on the current enabled day. Disabled dates already stay blocked by selection logic, so unavailable days no longer change value by click or keyboard.
Validation note: `npm run lint`, `npm run typecheck`, and `npm test` passed.

TASK-001 done.
DatePicker scaffold already present and matches PRD structure.
Verified `src/components/date-picker` tree, `index.ts`, `DatePicker.module.css`, and all layer folders/files exist.
Ran `node scripts/validate-date-picker-structure.mjs`; pass.
Next step: move to next pending feature only after this commit.

TASK-042 done.
Added real DOM test harness in `scripts/date-picker-test-harness.mjs`.
Runner now transpiles TS/TSX/CSS source through require hooks, installs mini DOM/window globals, and exposes `render`, `screen`, `user`, and jest-dom style matchers to VM tests.
Added `src/components/date-picker/tests/dom-stack.test.tsx` to prove render, query, click, tab, visibility, focus, and attribute assertions in integration mode.
Validation note: `npm test`, `npm run lint`, and `npm run typecheck` all pass.

TASK-031 done.
Added live region support to DatePicker with `role="status"` and `aria-live="polite"`, plus controlled a11y state wiring for dialog, grid, and day cells.
Selection and open flows now keep centralized a11y helpers in use, and DOM coverage checks the live region on open.
Validation note: `npm run lint`, `npm run typecheck`, and `npm test` passed.
Next step: TASK-028 if responsive/mobile accessibility is next.

TASK-043 done.
Browser-level a11y and interactive flow tooling already existed, so I verified and locked it in instead of rebuilding it.
`npm run test:a11y` and `npm run test:browser` both pass here with Chromium fallback.
Next step: TASK-032 if helper unit coverage is next.

TASK-028 done.
DatePicker styles now adapt for narrow viewports with stacked field controls, full-width dialog, larger touch targets, and explicit focus-visible rings.
Browser check script now loads the DatePicker CSS and verifies desktop/mobile responsive layout when Chromium is available.
Validation note: `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` passed. Chromium browser check blocked here, so the script used DOM fallback.

TASK-033 done.
Added helper unit coverage for input, i18n, a11y, and validation layers.
New tests hit sanitize/parse/format, two locales, aria text builders, and validation priority in real runtime checks.
Validation note: `npm run test:unit`, `npm run lint`, and `npm run typecheck` passed.
